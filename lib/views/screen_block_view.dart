import 'package:flutter/material.dart';

class BlockViewScreen extends StatelessWidget {
  const BlockViewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
